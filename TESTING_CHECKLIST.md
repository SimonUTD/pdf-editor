# PDF Editor - Phase 5 功能测试清单

**测试日期:** 2026-01-30
**测试版本:** Phase 5 Stage 1-2 完成
**测试范围:** 基础架构 + 查看增强功能

---

## 📋 测试前准备

### 环境检查
- [x] Node.js 版本正确 (18+)
- [x] pnpm 已安装
- [x] 依赖已安装 (`pnpm install`)
- [x] 应用可正常启动 (`pnpm run electron:dev`)

### 测试文件准备
- [x] 准备测试PDF文件（建议多个）：
  - [x] 小文件（1-5页）
  - [x] 中等文件（10-20页）
  - [x] 大文件（50+页）
  - [x] 包含文本的PDF
  - [x] 包含图片的PDF
  - [x] 有旋转页面的PDF
  - [x] 中文PDF
  - [x] 英文PDF

---

## 🧪 Phase 5 Stage 1: 基础架构测试

### 1. 服务层结构测试

#### 文件结构检查
- [ ] `src/services/pdf/` 目录存在
  - [ ] 包含13个占位符服务文件
  - [ ] 每个文件都有TODO注释
- [ ] `src/services/viewer/` 目录存在
  - [ ] ViewModeService.ts 存在
  - [ ] NavigationService.ts 存在
- [ ] `src/services/viewer/index.ts` 导出正确
- [ ] `src/services/pdf/index.ts` 导出正确

**预期结果:**
- 所有目录创建成功
- barrel文件正确导出服务
- TypeScript无导出错误

---

### 2. ViewModeService 测试

#### 功能测试
- [ ] 导出类型 `ViewMode` 定义为: 'actual' | 'fit-page' | 'width100%' | 'two-page'
- [ ] `calculateZoom()` 方法存在
- [ ] `isSpecialMode()` 方法存在

#### 逻辑测试（手动验证）
1. **actual 模式:**
   - 页面尺寸: 800x600, 容器: 1000x800
   - 预期返回: 1.0 (100%缩放)

2. **fit-page 模式:**
   - 页面: 800x600, 容器: 1000x800
   - 预期返回: min(1000/800, 800/600) = min(1.25, 1.33) = 1.25

3. **fit-width 模式:**
   - 页面宽度: 800, 容器宽度: 1000
   - 预期返回: 1000/800 = 1.25

4. **two-page 模式:**
   - 页面: 800x600, 容器: 1000x800
   - 两页宽度: 800*2+20 = 1620
   - 预期返回: min(1620/1000, 800/600) = 1.33

**预期结果:**
- 所有计算公式正确
- TypeScript编译通过

---

### 3. NavigationService 测试

#### 功能测试
- [ ] `validatePageNumber()` 方法存在
- [ ] `jumpToPage()` 方法存在
- [ ] `getPageDisplayText()` 方法存在

#### 逻辑测试
1. **页码验证:**
   - 输入: 5, 总页数: 10 → 预期: 5
   - 输入: 0, 总页数: 10 → 预期: 1 (最小值)
   - 输入: 15, 总页数: 10 → 预期: 10 (最大值)
   - 输入: -5, 总页数: 10 → 预期: 1 (修正后)

2. **显示文本:**
   - 输入: 5, 总页数: 20 → 预期: "5 / 20"
   - 输入: 1, 总页数: 1 → 预期: "1 / 1"

**预期结果:**
- 所有边界情况正确处理
- 显示文本格式正确

---

### 4. ToolsPanel 组件测试

#### UI 测试
启动应用后检查：
- [ ] 工具栏有"工具箱"按钮
- [ ] 点击按钮后打开右侧侧边栏（320px宽）
- [ ] 侧边栏标题显示"工具箱"
- [ ] 包含4个可折叠分类面板

#### 功能分类测试
- [ ] **查看增强** 分类
  - [ ] 查看模式
  - [ ] 快捷跳转
  - [ ] 文本搜索
- [ ] **转换工具** 分类
  - [ ] PDF转换器
  - [ ] 图片转PDF
  - [ ] 提取图像
- [ ] **编辑工具** 分类
  - [ ] PDF拆分
  - [ ] 页面提取
  - [ ] 页面重排
  - [ ] 添加页码
  - [ ] 标记密文
- [ ] **高级功能** 分类
  - [ ] PDF压缩
  - [ ] PDF签署
  - [ ] 密码保护
  - [ ] PDF优化

#### 交互测试
- [ ] 每个工具项可点击
- [ ] 点击后显示message提示（工具名称）
- [ ] 点击"文本搜索"打开搜索对话框
- [ ] 手风琴效果（一次只能展开一个分类）

**预期结果:**
- 所有17个工具入口显示正确
- 交互响应正常
- UI无布局问题

---

### 5. 状态管理测试

#### uiStore 新状态测试
- [ ] `viewMode` 状态存在，默认值 'fit-page'
- [ ] `showToolsPanel` 状态存在，默认值 false
- [ ] `searchQuery` 状态存在，默认值 ''
- [ ] `searchResults` 状态存在，默认值 []
- [ ] `currentMatchIndex` 状态存在，默认值 0

#### Actions 测试
- [ ] `setViewMode()` 方法存在
- [ ] `toggleToolsPanel()` 方法存在
- [ ] `setSearchQuery()` 方法存在
- [ ] `setSearchResults()` 方法存在
- [ ] `setCurrentMatchIndex()` 方法存在

**预期结果:**
- 所有状态和方法在uiStore中正确初始化
- TypeScript无类型错误

---

## 🧪 Phase 5 Stage 2: 查看增强功能测试

### 6. ViewModeSelector 组件测试

#### UI 测试
- [x] 工具栏显示"查看模式"下拉框
- [ ] 下拉框宽度约120px
- [x] 占位符显示"查看模式"
- [x] 有4个选项：
  - [x] "1:1"
  - [x] "完整页面"
  - [x] "宽度100%"
  - [x] "双页并排"

#### 功能测试
- [ ] 选择"1:1"后，PDF以100%大小显示
- [ ] 选择"完整页面"后，PDF缩放适应窗口
- [ ] 选择"宽度100%"后，PDF宽度填满窗口
- [ ] 选择"双页并排"后，显示两页（如果当前页是奇数页）
- [x] 切换模式时，PDF立即重新渲染

**测试步骤:**
1. 打开任意PDF文件
2. 尝试每种查看模式
3. 调整窗口大小，验证fit-page和fit-width是否响应
4. 验证双页模式只在奇数页时生效（1&2, 3&4等）

**预期结果:**
- 所有模式切换正常工作
- 缩放计算准确
- 无渲染错误

---

### 7. PageJumpControl 组件测试

#### UI 测试
- [x] 工具栏显示页码信息："当前页 / 总页数"
- [x] 页码格式为数字格式（如 "5 / 20"）
- [x] 有输入框用于输入页码
- [ ] 输入框宽度约70px
- [ ] 输入框占位符显示"跳转"

#### 功能测试
1. **有效跳转:**
   - [x] 输入有效页码（如"10"）
   - [x] 按Enter键或失去焦点时触发跳转
   - [x] PDF视图更新到目标页
   - [x] 缩略图高亮更新

2. **边界测试:**
   - [x] 输入0或负数 → 自动修正为第1页
   - [x] 输入超过总页数 → 自动修正为最后一页
   - [x] 输入小数 → 自动取整

3. **显示更新:**
   - [x] 跳转后"当前页"数字更新
   - [x] 缩略图侧边栏高亮对应页面

**测试步骤:**
1. 打开20页PDF
2. 测试各种输入：
   - 输入"1" → 跳转到第1页
   - 输入"20" → 跳转到第20页
   - 输入"0" → 跳转到第1页
   - 输入"25" → 跳转到第20页
   - 输入"5.5" → 跳转到第5页

**预期结果:**
- 所有边界情况正确处理
- 跳转响应快速
- 页码显示准确

---

### 8. TwoPageView 双页并排测试

#### UI 测试
- [ ] 选择"双页并排"模式后，显示两页
- [ ] 两页之间有20px间隙
- [ ] 两页都有阴影效果
- [ ] 如果是最后一页（偶数页），只显示一页

#### 功能测试
1. **页面组合:**
   - [ ] 当前是第1页 → 显示第1&2页
   - [ ] 当前是第3页 → 显示第3&4页
   - [ ] 当前是第5页 → 显示第5&6页
   - [ ] 当前是第20页（假设总共20页，偶数）→ 只显示第20页

2. **页面旋转:**
   - [ ] 双页模式下，每一页独立应用旋转
   - [ ] 旋转正确渲染

3. **缩放:**
   - [ ] 双页模式下缩放计算正确
   - [ ] 两页保持相同缩放比例

**测试步骤:**
1. 打开至少20页的PDF
2. 切换到"双页并排"模式
3. 翻页验证页面组合：
   - 第1页应显示 1&2
   - 第3页应显示 3&4
   - 第5页应显示 5&6
4. 旋转某页面，验证双页视图正确更新

**预期结果:**
- 页面组合逻辑正确
- 两页渲染质量良好
- 性能流畅无卡顿

---

### 9. PDF 文本搜索功能测试

#### PDFSearchService 测试

##### 功能测试
- [ ] `searchPDF()` 方法存在
- [ ] `clearCache()` 方法存在
- [ ] `getTotalMatchCount()` 方法存在

##### 搜索逻辑测试
1. **基本搜索:**
   - [ ] 搜索存在的文本 → 返回匹配项
   - [ ] 搜索不存在的文本 → 返回空数组
   - [ ] 搜索空字符串 → 返回空数组
   - [ ] 大小写不敏感（"Hello" = "hello"）

2. **缓存机制:**
   - [ ] 相同搜索词第二次查询使用缓存
   - [ ] PDF变化后缓存失效

3. **结果格式:**
   - [ ] 每个结果包含 `pageIndex`, `pageNumber`, `items`
   - [ ] 每个item包含 `text`, `transform`, `bbox`
   - [ ] bbox格式: [x0, y0, x1, y1]

**测试步骤:**
```javascript
// 在浏览器控制台测试
const { PDFSearchService } = require('@/services/pdf/PDFSearchService');

// 测试1: 搜索存在的文本
const results1 = await PDFSearchService.searchPDF(pdfDocument, 'test');
console.log('找到匹配:', results1.length, '页');

// 测试2: 搜索不存在的文本
const results2 = await PDFSearchService.searchPDF(pdfDocument, 'notfound');
console.log('未找到:', results2.length === 0);

// 测试3: 缓存
const results3 = await PDFSearchService.searchPDF(pdfDocument, 'test');
console.log('使用缓存:', results1 === results3);
```

**预期结果:**
- 搜索逻辑正确
- 缓存机制工作
- 结果数据结构正确

---

#### SearchPanel UI 测试

##### 交互测试
- [ ] 点击工具箱 → "文本搜索" → 打开对话框
- [ ] 对话框标题"文本搜索"
- [ ] 对话框宽度约400px
- [ ] 有搜索输入框（带搜索图标）
- [ ] 有"清除"按钮

##### 搜索功能测试
1. **基本搜索:**
   - [ ] 输入搜索词后300ms自动搜索
   - [ ] 显示loading状态
   - [ ] 找到匹配项显示："找到 X 个匹配项"
   - [ ] 未找到显示："未找到匹配项"
   - [ ] 搜索出错显示："搜索失败"

2. **结果导航:**
   - [ ] 显示 "当前匹配 / 总匹配" 格式
   - [ ] "上一个"按钮存在并可点击
   - [ ] "下一个"按钮存在并可点击
   - [ ] 点击"下一个" → currentMatchIndex + 1
   - [ ] 点击"下一个"到最后一项后循环到第一项
   - [ ] 点击"上一个" → currentMatchIndex - 1
   - [ ] 点击"上一个"到第一项后循环到最后一项
   - [ ] 导航时自动跳转到包含匹配项的页面

**测试步骤:**
1. 打开包含重复文本的PDF（如"test"出现多次）
2. 打开文本搜索对话框
3. 输入"test"并等待搜索完成
4. 验证显示匹配数量
5. 点击"下一个"按钮多次，验证：
   - 页面跳转正确
   - 匹配计数器更新
   - 到达末尾后循环到开头
6. 点击"上一个"按钮验证反向导航

**预期结果:**
- 搜索响应快速（大文件可能稍慢）
- 导航逻辑正确
- 页面跳转流畅

---

#### 搜索高亮测试

##### Canvas 高亮渲染
- [ ] 搜索到的文本在PDF上高亮显示
- [ ] 当前匹配项显示为**橙色**（rgba(255, 200, 0, 0.5)）
- [ ] 其他匹配项显示为**黄色**（rgba(255, 255, 0, 0.3)）
- [ ] 高亮位置准确（覆盖文本）
- [ ] 高亮在overlay canvas上绘制，不影响PDF内容

##### 高亮更新
- [ ] 切换页面后，高亮正确显示当前页匹配项
- [ ] 导航到下一个匹配项后，高亮更新
- [ ] 当前匹配项高亮颜色与其他不同

##### 性能测试
- [ ] 大量匹配项（100+）不卡顿
- [ ] 切换页面时高亮重新渲染流畅

**测试步骤:**
1. 搜索常见词（如"the", "and", "PDF"等）
2. 验证高亮显示位置准确
3. 验证当前匹配项颜色突出
4. 快速点击"下一个"/"上一个"按钮
5. 切换到其他页面，验证高亮消失

**预期结果:**
- 高亮位置精确覆盖文本
- 颜色区分明显
- 性能流畅无延迟

---

## 🔧 兼容性测试

### 跨组件集成测试
- [ ] 查看模式 + 页面跳转 → 正常工作
- [ ] 查看模式 + 文本搜索 → 正常工作
- [ ] 双页模式 + 页面跳转 → 正常工作
- [ ] 所有模式 + 页面旋转 → 正常工作

### 状态同步测试
- [ ] 切换查看模式时，zoom状态正确更新
- [ ] 跳转页面时，selectedPageIndex正确更新
- [ ] 搜索时，searchResults正确更新
- [ ] 导航搜索结果时，currentMatchIndex正确更新

### 边界情况测试
- [ ] 打开单页PDF，所有功能正常
- [ ] 打开100页大PDF，性能良好
- [ ] 打开没有文本的PDF，搜索返回空
- [ ] 快速切换查看模式10次，无错误
- [ ] 在双页模式翻到最后一页（偶数页）

---

## 🐛 已知问题记录

### 记录格式
```
[问题编号] 问题描述
- 严重程度: 低/中/高
- 复现步骤: ...
- 预期行为: ...
- 实际行为: ...
```

### 示例
```
[STAGE2-001] 双页模式在偶数页时只显示一页
- 严重程度: 低
- 复现步骤: 打开20页PDF，翻到第20页，切换双页模式
- 预期行为: 只显示第20页（因为第21页不存在）
- 实际行为: 正确显示第20页
- 状态: 符合预期，非bug
```

---

## ✅ 测试完成标准

### 必须通过（P0）
- [ ] 所有查看模式可正常切换
- [ ] 页面跳转功能正常
- [ ] 文本搜索可找到匹配项
- [ ] 搜索结果可导航
- [ ] 搜索高亮正确显示
- [ ] 无JavaScript错误或崩溃

### 建议通过（P1）
- [ ] 双页模式渲染良好
- [ ] 大文件性能可接受
- [ ] UI响应流畅

### 可选优化（P2）
- [ ] 搜索性能可优化
- [ ] 高亮渲染更精确

---

## 📝 测试报告模板

```markdown
## Phase 5 Stage 2 测试报告

**测试人:** ______
**测试日期:** ______
**测试环境:** macOS / Windows
**Electron版本:** ______
**PDF测试文件:** ______

### 测试结果总结
- 通过: ___ / ___
- 失败: ___ / ___
- 阻塞: ___ / ___

### 详细测试结果

#### 6. ViewModeSelector
- [ ] UI测试: ✅ / ❌
- [ ] 功能测试: ✅ / ❌
- [ ] 备注: ______

#### 7. PageJumpControl
- [ ] UI测试: ✅ / ❌
- [ ] 功能测试: ✅ / ❌
- [ ] 备注: ______

#### 8. TwoPageView
- [ ] UI测试: ✅ / ❌
- [ ] 功能测试: ✅ / ❌
- [ ] 备注: ______

#### 9. PDFSearch
- [ ] Service测试: ✅ / ❌
- [ ] UI测试: ✅ / ❌
- [ ] 功能测试: ✅ / ❌
- [ ] 备注: ______

### 发现的问题
1. **问题描述**
   - 严重程度: 低/中/高
   - 复现步骤: ...
   - 截图: ______

### 改进建议
- ______
- ______
```

---

## 🚀 测试后行动

### 如果所有测试通过 ✅
- 标记Stage 2为生产就绪
- 继续Stage 3开发
- 更新用户文档

### 如果发现bug 🐛
- 记录bug详情
- 评估修复优先级
- 修复后重新测试
- 提交修复commit

---

**测试完成后，请提供反馈，我将根据您的测试结果进行必要的修复和优化！**

**祝测试顺利！** 🎯
